<nav class="navbar navbar-expand-lg navbar-dark bg-dark d-flex" data-bs-theme="dark">
	<div class="container-fluid mx-4">
		<a class="navbar-brand" href="#"><img class="logo-navbar" src="{{ asset('build/artoyz-logo.svg') }}" alt="artoyz-logo"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarText">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0  d-flex align-items-center">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_product') }}" aria-label="link to products section">HOME</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_toys') }}" aria-label="link to toys section">TOYS</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_brands') }}" aria-label="link to brands section">BRANDS</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_favorites') }}" aria-label="link to favorites section">FAVORITES</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_dashboard') }}" aria-label="link to dashboard">DASHBOARD</a>
					</li>
				{% endif %}
				{% if app.user %}
					{{ app.user.email }}
					<li class="nav-item account">
						<a class="nav-link" href="{{ path('app_logout') }}" aria-label="link to logout">LOGOUT</a>
					</li>
				{% else %}
					<li class="nav-item account">
						<a class="nav-link" href="{{ path('app_login') }}" aria-label="link to login">LOGIN</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
