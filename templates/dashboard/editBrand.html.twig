{% extends 'base.html.twig' %}
{% form_theme brandEditForm 'bootstrap_5_layout.html.twig' %}

{% block title %}Edit brands{% endblock %}

{% block body %}
    <body class="dashboard-body">
        <main class ="container m-auto">
            <div class="row m-5">
                <h1>EDIT {{ brand.name }}</h1>
                {{ form_start(brandEditForm) }}
                {{ form_widget(brandEditForm) }}
                <button class="btn btn-dark">{{ button_label|default('Save') }}</button>
                {{ form_end(brandEditForm) }}
            </div>
        </main>
    </body>
{% endblock %}
